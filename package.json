{
  "name": "sillytavern-triggeryze",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:run": "vitest run",
    "test:rulesets": "vitest run --config vitest.rulesets.config.js"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "vitest": "^2.1.0"
  }
}
